type Query {
  me: Me
}

type Me {
  user: User!
  groupIds: [String!]!
  identities: [Identity!]!
  discordServers: [DiscordServer!]!
}

type User {
  id: String!
  name: String!
  icon: String
  createdAt: String!
}
