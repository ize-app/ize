type Query {
  users: [User]
  me: Me
}

type Me {
  id: String!
  name: String
  discordData: DiscordData!
}

type User {
  id: String!
  name: String!
  icon: String
  createdAt: String!
  discordData: DiscordData!
}
