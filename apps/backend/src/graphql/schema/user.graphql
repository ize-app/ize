type Query {
  me: Me
}

type Mutation {
  updateProfile(profile: UpdateProfileArgs!): Boolean!
}

type Me {
  user: User!
  identities: [Identity!]!
  discordServers: [DiscordServer!]!
}

type User {
  id: String!
  name: String!
  icon: String
  createdAt: String!
}

input UpdateProfileArgs {
  name: String!
}
