# mirrors Prisma type + "None" type
enum ActionNewType {
  CallWebhook
  TriggerStep
  EvolveFlow
  None
}

input CallWebhookArgs {
  uri: String!
  name: String!
}

input ActionNewArgs {
  type: ActionNewType!
  filterOptionIndex: Int
  callWebhook: CallWebhookArgs
}
