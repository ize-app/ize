type Permission {
  stepTriggered: Boolean!
  anyone: Boolean!
  entities: [Entity!]!
}

union Entity = Identity | Group

input PermissionArgs {
  anyone: Boolean!
  entities: [EntityArgs!]!
  userId: String
}
