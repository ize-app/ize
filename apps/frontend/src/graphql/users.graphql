query Users {
  users {
    ...UserParts
  }
}

query Me {
  me {
    ...UserParts
  }
}

fragment UserParts on User {
  id
  name
  discordData {
    ...DiscordDataParts
  }
}

fragment DiscordDataParts on DiscordData {
  id
  username
  discriminator
  avatar
}
